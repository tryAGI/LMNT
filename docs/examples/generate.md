# Generate

Basic example showing how to create a client and make a request.

This example assumes `using LMNT;` is in scope and `apiKey` contains your LMNT API key.

```csharp
using var client = new LmntClient(apiKey);
```